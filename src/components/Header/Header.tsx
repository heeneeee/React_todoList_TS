import { StHeader, StTitle } from "./style";

export default function Header() {
  return (
    <div>
      <StHeader>
        <StTitle>Heewon's TodoList</StTitle>
      </StHeader>
    </div>
  );
}
